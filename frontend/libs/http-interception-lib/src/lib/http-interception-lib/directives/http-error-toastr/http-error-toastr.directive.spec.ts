import { HttpErrorToastrDirective } from './http-error-toastr.directive';

describe('HttpErrorToastrDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpErrorToastrDirective();
    expect(directive).toBeTruthy();
  });
});
